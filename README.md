# 🚚 প্রোডাক্ট ট্র্যাকিং — MERN অ্যাপ

**লাইভ ডেমো:** [Primary Demo](https://product-tracker-f.onrender.com) | [Secondary Demo](https://product-tracker-f.onrender.com)

এটি একটি সহজ এবং কার্যকর **প্রোডাক্ট ট্র্যাকিং অ্যাপ**, যা MERN স্ট্যাক (MongoDB, Express, React, Node) ব্যবহার করে তৈরি করা হয়েছে। এখানে অ্যাডমিন নতুন প্রোডাক্ট যোগ করতে পারে, একাধিক প্রোডাক্ট সিলেক্ট করে একসাথে স্ট্যাটাস আপডেট করতে পারে — দ্রুত ট্র্যাকিং এবং বাল্ক স্ট্যাটাস ম্যানেজমেন্টের জন্য উপযুক্ত।

---

## ✨ মূল বৈশিষ্ট্যসমূহ (Features)

- 🔹 **অ্যাডমিন কন্ট্রোল** — অ্যাডমিন নতুন প্রোডাক্ট যোগ করতে পারবে (নাম, ট্র্যাকিং নম্বর, ট্রান্সপোর্ট, স্ট্যাটাস ইত্যাদি)।
- 🔹 **বাল্ক স্ট্যাটাস আপডেট** — একাধিক প্রোডাক্ট সিলেক্ট করে একসাথে স্ট্যাটাস পরিবর্তন করা যাবে (যেমন `Pending`, `In Transit`, `Delivered` ইত্যাদি)।
- 🔹 **প্রোডাক্ট ডিটেইল ভিউ** — প্রতিটি প্রোডাক্টের বর্তমান অবস্থা, পূর্বের স্ট্যাটাসের ইতিহাস, এবং অন্যান্য তথ্য দেখা যাবে।
- 🔹 **রিসেন্ট অ্যাকটিভিটি** — সর্বশেষ আপডেট হওয়া এবং নতুন যোগকৃত প্রোডাক্টগুলোর তালিকা দেখা যাবে।
- 🔹 **অনুসন্ধান ও ফিল্টার** — Tracking Number, Name বা Status অনুযায়ী দ্রুত সার্চ করা যাবে।
- 🔹 **ওয়ান-পেজ অ্যাপ (SPA)** — সব কাজ এক পেজ থেকে করা যায়।
- 🔹 **REST API** — Backend এ CRUD এবং বাল্ক স্ট্যাটাস আপডেট API রয়েছে।

---

## 🔗 লাইভ ডেমো

অনলাইনে অ্যাপটি দেখুন:  
[Primary Demo](https://product-tracker-f.onrender.com) | [Secondary Demo](https://product-tracker-f.onrender.com)

---

## 🛠️ ব্যবহৃত প্রযুক্তি (Tech Stack)

| টেকনোলজি      | ব্যবহার                      |
| ------------- | ---------------------------- |
| MongoDB       | প্রোডাক্ট ডেটা সংরক্ষণ       |
| Express.js    | ব্যাকএন্ড API                |
| Node.js       | সার্ভার রানটাইম              |
| React (Vite)  | ফ্রন্টএন্ড (Single Page App) |
| Axios / Fetch | API কল                       |

---

## 📦 ইন্সটলেশন ও রান করার নিয়ম

```bash
# রিপোজিটরি ক্লোন করুন
git clone https://github.com/joy094/Product_Tracker
cd product-tracking

# Backend সেটআপ
cd Product_Tracker
npm install
# .env ফাইলে যোগ করুন:
# MONGO_URI=your_mongo_atlas_url
# PORT=5000
npm run dev  # অথবা node server.js

# Frontend (Vite) সেটআপ
cd client
npm install
# .env.development এ যোগ করুন:
# VITE_API_URL=http://localhost:5000
npm run dev
product-tracking/


```
